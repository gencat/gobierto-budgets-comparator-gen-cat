<%= link_to_unless_current(
      budget_line_denomination(@area_name, budget_line['code'], budget_line['kind']),
      location_budget_line_path(
        current_organization.combined_slug,
        budget_line['year'],
        budget_line['code'],
        budget_line['kind'],
        @area_name
      )
    ) %>
